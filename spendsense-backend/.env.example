# Backend Environment Variables Example
# Copy this file to .env and fill in the actual values

# Database Configuration
# Get the connection string from AWS Secrets Manager:
# aws secretsmanager get-secret-value --secret-id spendsense/database/connection --query SecretString --output text
# Or construct manually: postgresql://username:password@host:5432/database_name
# For local development, you may need to set up port forwarding or use a local database
# Database endpoint from CDK output: aws cloudformation describe-stacks --stack-name SpendSense-Database-{env} --query 'Stacks[0].Outputs[?OutputKey==`DatabaseEndpoint`].OutputValue' --output text
DATABASE_URL=postgresql://username:password@host:5432/spendsense

# AWS Cognito Configuration
# Get these values from AWS CDK stack outputs:
# - User Pool ID: aws cloudformation describe-stacks --stack-name SpendSense-Cognito-{env} --query 'Stacks[0].Outputs[?OutputKey==`UserPoolId`].OutputValue' --output text
# - Client ID: aws cloudformation describe-stacks --stack-name SpendSense-Cognito-{env} --query 'Stacks[0].Outputs[?OutputKey==`UserPoolClientId`].OutputValue' --output text
# Or from AWS Secrets Manager: aws secretsmanager get-secret-value --secret-id spendsense/cognito/configuration
COGNITO_USER_POOL_ID=your-cognito-user-pool-id
COGNITO_CLIENT_ID=your-cognito-client-id

# AWS Configuration
AWS_REGION=us-east-1

# LLM API Keys
# Get your API key from https://platform.openai.com/api-keys or https://console.anthropic.com/
# You only need one of these, depending on which LLM provider you're using
OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
